# Claude Code instructions

Claude Code does not discover `AGENTS.md` on its own, so this file imports it. `AGENTS.md` is the single
source of project instructions for every agent; edit that file, not this one.

@AGENTS.md
