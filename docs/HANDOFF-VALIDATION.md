# Historical revision-2 preparation validation

For the current revision see [ACCELERATOR-VALIDATION.md](ACCELERATOR-VALIDATION.md).
The counts and nonimplementation statements below describe revision 2 only.

**Revision:** 2, GC420d / USB / Tahoe 26 / local signing
**Date:** 2026-09-15
**Scope:** specifications and starter scaffold, not an implemented printer driver.

## Environment

Linux x86_64, Swift 6.2.1 (target `x86_64-unknown-linux-gnu`), Python 3.13.5 and PyYAML 6.0.3. This environment is not macOS and has no attached printer. Results do not establish Apple Silicon execution, Apple SDK compatibility, installed scheduler behavior, browser interaction, physical output, code-signature admission or installation.

## Checks actually run on this revision

| Check | Result | Boundary |
|---|---|---|
| `swift test --package-path Packages/LabelCore` | 8 XCTest methods passed; zero failures | Debug portable bitmap-layout scaffold, including GC420d packed-layout regression; Linux x86_64 |
| `swift test --package-path Packages/LabelCore --configuration release` | The same 8 methods passed; zero failures | Optimized portable scaffold, not a driver or physical geometry calibration |
| `swift package --package-path Packages/LabelMac dump-package` | Manifest parsed with macOS 26.0 minimum | Does not compile Core Graphics or execute Mac code |
| `python3 -m unittest discover -s scripts/tests -v` | 20 tests passed | Six original repository/change-scope tests plus fourteen reference-seed/rounding/constraint tests |
| `python3 scripts/check_reference_target.py` | Passed | Confirmed planning inputs; rational dot-pitch oracle; preserved unknowns and zero authorizations |
| `bash -n` on each of the three shell scripts independently | All three passed | Syntax only |
| Host preflight, local-signing smoke and Mac CI script invoked on Linux | Each refused with expected exit code 2 | Platform guards exercised; no Mac command, signing operation, installation or device access followed |
| YAML parsing of two workflows and Dependabot | Three files parsed with PyYAML BaseLoader | Basic syntax/expected structure, not actionlint or an Actions run |
| Actual `ci-required` shell extracted from YAML | All 64 combinations matched expected pass/fail | Local Bash `-e -o pipefail`; repository result × scope flag × Mac result; not Actions scheduler validation |
| Acceptance/requirement consistency | 90 acceptance IDs across seven milestones; 21 mapped requirements | IDs/order/descriptions agree with metadata; all 38 review-sized slices retained |
| `python3 scripts/check_repo.py` | Passed | Required files, links, reference anchors, JSON, progress/scope consistency, platform declarations, workflow pin syntax |

Swift's separate Swift Testing runner reports zero Swift Testing tests because the scaffold uses XCTest. Debug and release are two configurations of eight distinct XCTest methods, not sixteen different cases.

An erroneous duplicate `platforms` argument in the revised LabelCore manifest was caught during compilation, removed, and both configurations were rerun successfully. Final results above describe the corrected package.

## Source verification and scope

Manufacturer model specifications and public Zebra protocol references were consulted; the relevant model-spec PDF pages were inspected visually. Apple code-signing/Gatekeeper references and GitHub's macOS 26 standard-runner announcement were checked. See [REFERENCES.md](REFERENCES.md). These establish documented inputs, not behavior of the user's exact firmware or OS build.

The existing action commit pins were retained from revision 1; their SHA format was checked here. They were not newly resolved from upstream releases during this revision. Runner labels do not freeze future image contents, so the Mac preflight records the observed toolchain/image.

No GitHub API mutation, repository creation, branch/issue/PR/release publication or Actions execution was performed in this revision. The earlier handoff's 404 lookup remains a historical absent-or-inaccessible result, not proof of current repository state.

## Not run and not implied

No macOS compilation/execution, `codesign` verification, Gatekeeper or quarantined-download check, privileged/helper installation, actual CUPS queue/filter execution, system print dialog, browser workflow, USB transfer, physical label, barcode scan, cutter/peeler operation, update/uninstall test or notarization was performed.

The local-signature script signs only the inert diagnostic when run on a suitable Mac. Its syntax/platform guard has been checked here; its Mac path still needs execution. Adding that script does not implement a secure installer or prove spooler admission.

`Fixtures/catalog.json` still describes 24 fixture families to implement. No synthetic PDF/image corpus was generated by this revision. The GC420d planning seed is deliberately unqualified, with unknown observations and no hardware/install authority. It is not a completed production capability profile.

Global and scoped progress remain unfinished. These scaffold checks do not satisfy M0's remote/ARM gates or any M1–M6 product criterion. Codex must record evidence against actual implementation commits and the exact local Mac/printer configuration.

## Archive integrity

The complete revision-2 archive contains 92 files, including 62 Markdown documents. It excludes build products, logs, caches, private inputs and temporary preparation scripts. `MANIFEST.sha256` covers every packaged file except itself. ZIP CRC and each manifest digest were verified after packaging. This is a content-integrity check, not a signature authenticating the publisher.
