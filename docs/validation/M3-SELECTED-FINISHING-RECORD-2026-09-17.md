# Selected finishing record lookup — 2026-09-17

This local, unpublished slice advances setup-app integration. A selected accepted-record
file supplies the catalog root and typed reference, without asking the user to transcribe
IDs or hashes. The reader checks an existing private owned catalog, reads through the
immutable directory helper without namespace creation, parses bounded canonical metadata,
and verifies the filename against the embedded acceptance identity. Catalog inode and
permissions must remain stable through lookup.

The selector and ordinary store reopen share one record parser. The nearest independent
constraint is that selection is lookup only: a fresh full original-source/context reopen
must precede validated display, preparation or export. The returned type grants no delivery,
replay or hardware-completion authority. No GUI action is integrated in this slice yet.

A native regression exercises exact reference derivation and full accepted-job reopen,
wrong filename, UInt64.max metadata length, symlink input and unsafe catalog permissions.
Initial finite focused session92802 failed one URL-equality assertion: directory URL
trailing-slash metadata differed while filesystem paths were identical. The assertion now
compares paths. Full context reopen and rejection checks passed in that run. Filename-check
omission fault failed exit1 through the expected assertion; exact restored32-case run
passed own exit0 in session29989. Full finite900-second Mac gate session32797 failed exit1: debug356 native cases passed,
release356 had one existing source-page-preview nil result. That unchanged four-test
release suite passed on finite rerun session77654. The failed gate also logged477 seconds
for a one-second bounded loopback case; host scheduling/suspension delay is a hypothesis,
not established cause. No deadline or criterion was relaxed. Subsequent full gate14214 passed exit0:104Python/281Core/358Mac debug/release plus
independent/inert/signature/packaged checks; artifact artifacts/setup-app.gLgyA5.
Source checkpoint pending.

Installed scheduler, retail installation, GUI/VoiceOver, unit identification/status and
physical printing remain open. Frozen Part B unchanged. No merge, publishing, privilege
or printer I/O occurred.
