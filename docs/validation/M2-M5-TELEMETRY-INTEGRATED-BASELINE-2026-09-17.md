# Integrated telemetry software baseline

Exact local unpublished source: `3be5feca11d89c0987f4106ffaea5760b07df4df`. Working tree clean before and after.
Finite900-second caffeinate-protected `bash scripts/ci-swift.sh` completed exit0.
This baseline includes the in-worker RSS/result validation, CLI/benchmark transport
of optional metrics, localization hooks, chooser-failure authority revocation and
export disclosure. It supersedes earlier suite counts for current source only.

-106 Python tests passed.
-281 LabelCore tests passed in each debug/release mode.
-361 LabelMac tests passed in debug (80.804s) and release (68.978s).
-Each mode:132 strict and180 independent compression round-trips,12 finite
 encoding CLI cases,15 CUPS ABI cases,14 filter ABI cases,one discard pipeline.
-ARM/minimum26 metadata, nested local-ad-hoc signature verification, explicit
 unavailable Developer-ID rejection, and packaged-worker PBM/ZPL equivalence passed.
-Local app: `artifacts/setup-app.HZUdFj/Label Printer Driver Setup.app` in the
 isolated checkout. No release or publication occurred.

Mac27 ARM runtime evidence does not establish runtime26 compatibility, Gatekeeper,
clean-account installation or scheduler/USB behavior. Renderer telemetry remains
an optional bounded per-process peak through preparation, not aggregate memory.
There was zero printer access, queue/default change, privilege action or manual GUI.

Traceability readback before the run was clean and not ready for maintainer
acceptance, with0 current declared criterion passes: older exact-source records
are historical following source changes. This suite result does not refresh any
per-ID ledger record without a deliberate semantic assessment.

Open gates: frozen M1 PartB and broader scheduler/ownership admission; qualified
USB/backend cancellation/lifetime; native installer authorization, code identity,
update/rollback/repair/uninstall; installed printing after window close/restart;
GUI/native dialogs/keyboard/VoiceOver; actual minimum-runtime compatibility;
physical image/scan quality, state isolation and fault/finishing qualification.
Source/packaging support claims and broader S2 hardware scope remain unqualified.
Next safe work is deliberate remaining requirement/evidence assessment against
actual assertions and integration boundaries, not another identical baseline run.
