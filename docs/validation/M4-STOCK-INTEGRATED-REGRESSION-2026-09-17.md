# Stock-opening integrated regression and stop — 2026-09-17

Evaluated unpublished checkpoint: `00d1026060a0ea23cb92d2fcaccef2d0e486f04e`, documentation descendant of stock-control source ed50407.

Finite900second full ci-swift wrapper TERMINATED with own exit1; session54681 is no longer live. Log: /tmp/zpl-stock-current-full-native.log. Both accelerator modes passed106Python/305Core plus independent oracles/inert cases. Full native debug executed379tests with4assertion failures in2tests. Native release and later script stages were NOT RUN after debug failure. Earlier local app build and35focused native tests remain valid narrow evidence, not a full baseline.

Failed tests in WorkflowDocumentOpeningModelTests:
- testSavedWorkflowCannotChangeTheConfirmedOutputStock
- testEqualDimensionsNeverAuthorizeForeignStockIdentity

Both expected no installed editor and the configured4×6 stock-mismatch message; both received an editor and nil error. The unrestricted offline bootstrap saved-stock admission also served the default configured-setup opening model. Stock identity is independent of dimensions: equal geometry must not implicitly satisfy configured-media identity. The focused suites omitted this caller and therefore missed the combined constraint.

Stop rule: this is a regression introduced by our preceding source change. No additional source fix is committed in this round. Do not weaken those default configured-opening tests or claim acceptance. Proposed restructure: explicit configured-setup versus offline-candidate stock opening policies. Default configured opening retains the exact identity/dimension guard; explicitly selected offline editing validates finite rendering budgets and starts an unqualified correction, without changing configured stock or qualification. Both public caller paths and both identity/dimension constraints must be tested together before resuming integrated validation. Review app caller wiring before selecting policy.

Published PR CI runs are on older remote heads and do not cover this local regression. No merge, source push, release, hardware or privileged action. Frozen Part B candidate unchanged.
