# Current integrated software baseline — 2026-09-17

Evaluated clean local unpublished source `d41f091681e8986691107419e1667c6b01cc4559`. HEAD and clean status were read back after the run. Finite900-second process-group wrapper executed `/usr/bin/caffeinate -i bash scripts/ci-swift.sh`; command exit0. Timeout handling would terminate only the owned process group; no timeout occurred.

## Environment and actual checks

Actual host: macOS27.0 build26A428, ARM64; Xcode27.0 build27A266a; Apple Swift6.4 (swiftlang6.4.0.34.1). Compilation/minimum-binary metadata targets26.0; actual retail26 runtime is NOT qualified by this27.0 host.

- 106 Python tests,303 Core tests and374 native tests passed in each debug/release stage, zero failures. Swift Testing zero-case boilerplate was not used as an oracle.
- Each accelerator mode passed132 strict vectors,180 compression round trips,2 real lab privacy cases,12 finite encoding CLI cases,15 inert CUPS ABI,14 inert filter ABI and1 filter-to-discard case.
- Repository/fixture/traceability checks passed. Fixture manifest:19 PDFs/29 pages plus3 standalone HTML files. Historical ledger records were validated, not refreshed as current acceptance.
- Local filter ARM/minimum26 metadata validated without system-state changes. Native executable ARM slices read back for diagnostic, CLI and worker.
- Local ad-hoc diagnostic/CLI/worker/supervision-fixture signatures verified. Selected unconfigured Developer-ID mode failed explicitly without fallback. App and both nested executables verified locally.
- Packaged worker synthetic PBM/ZPL exactly matched the release worker. No printer accessed.

Artifact: `artifacts/setup-app.DVSdSn/Label Printer Driver Setup.app`; log `/tmp/zpl-privacy-integrated-full.log`, SHA256 `e39a13cb417ca5d68d11d4744bb1389edcb7f6921611599f1a8fc1e098f1483d`; source pointer `/tmp/zpl-privacy-integrated-source.txt`. This new app does not replace or inherit approval for the frozen PartB candidate.

## Frozen Linux handoff

New source-only archive `/tmp/zpl-privacy-current-linux-source.tar`:533 regular tracked files,4218880 bytes; SHA256 `2e99ee453d08936221254e6df3c78833e9544da12994fc574d44f9d825e0aec2`. Archive file bytes independently matched every git-tree blob at the evaluated source. Git history and untracked local files/builds excluded; no universal secret/private-content absence claim. `/tmp/zpl-privacy-current-linux-handoff.md` contains finite commands, expected observed portable counters and excluded operations. Earlier archive remains frozen. No Linux worker launched or result claimed.

## Remaining boundaries

This software baseline integrates recent page-box extent, shared worker bitmap and request-canvas, integer admission, error identifier redaction, localized editor error and lab path-redaction changes. It is not a new whole M3-AC12 semantic acceptance assessment, benchmark qualification, hosted CI or release pass.

GUI/keyboard/VoiceOver, translated locale, quarantined launch, actual minimum-runtime26, installed scheduler/administrator/helper, cross-queue production integration, USB identity/lifetime/status/recovery, physical labels/accessories and two-build installed update/rollback evidence remain NOT RUN/open. Production adapter/helper still depend on M1 admission evidence; full-system ordinary application printing is not implemented/accepted by these engine tests. No queue/install/device write, merge, publication or ledger refresh. Source remains unpublished beyond PR81's older hosted head; primary checkpoint is an ancestor and its checkout is untouched. Next: current privacy/source-integration review, preserving manual/hardware gates and frozen candidate.
