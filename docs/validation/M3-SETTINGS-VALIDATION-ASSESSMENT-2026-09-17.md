# M3-AC02 settings validation assessment

Criterion: every explicit choice is range/combination checked; unsupported options
fail instead of silently clamping or dropping. Required A. Assessment: PASS for
the current typed software path at executed source `3be5feca11d89c0987f4106ffaea5760b07df4df`. No printer qualified.

The finite integrated gate passed106Python/281Core/361Mac in stated modes. Current
source differs only in evidence metadata. The following inspection covers actual
selected-control validation and negative assertions, not suite counts alone.

PrinterControlResolution chooses per-field job, workflow, then immutable configured
defaults, constructs an effective typed request, and validates it before constructing
resolved controls. Read-only observations never enter this chain. Ordinary and
finishing routes share non-finishing validation; the latter additionally requires
the exact reproducible plan and mode. Shadowed defaults remain shadowed by the
specified precedence, not a fallback that rescues an invalid selected value.

ConfiguredPrinterDefaultsTests asserts all priority levels and rejects unsupported
or unqualified configured defaults and invalid explicit speed despite valid defaults.
MotorSpeedIntegrationTests checks independent fields, unknown versus unsupported,
incomplete tuples and versioned capability/codec constraints. DarknessIntegrationTests
checks every integer0..30 for exact encoding and rejects Int.min,-1,31,Int.max,
unqualified/legacy profiles and too-small output budgets without partial fragments.
The prepared encoder independently refuses caller-declared baseline speeds beyond
its documented2/3/4 subset.

Geometry integration enumerates each absent/invalid independent bound, validates
per-field precedence and rejects conflicting tracking/continuous-length choices.
Known raster clipping fails at both prepare and direct encode. Offset integration
checks canonical profile fields, signed clipping, black-mark offset requirements,
and conflicting tracking/length combinations. Thermal integration enumerates both
methods: an incompatible explicit or selected workflow method rejects; a valid
higher-priority method may override a shadowed incompatible default by design.
Neither encoder path silently falls back from the invalid selected method.

Finishing control tests require mode support, enabled configuration and observed
accessory presence, plus independent qualification for each schedule. Invalid batch
sizes, output counts and missing policy declarations reject. The documented encoder
checks conflicting/duplicate kinds, explicit model/protocol intersections and bounded
output. Reference cut/peel/rewind/transfer and unqualified darkness/tracking/geometry/
offsets remain rejected. Normalization is not clamping or silent omission.

The software result does not validate this unit's settings, actual system-dialog
propagation, physical state isolation or hardware finishing. Those gates remain
open. No calibration, persistent change, queue/default mutation, printer query/write,
GUI/admin action, merge or publication occurred. No other criterion refreshed.
