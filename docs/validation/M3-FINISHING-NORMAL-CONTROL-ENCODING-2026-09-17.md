# Bound normal-control encoding for finishing preparation — 2026-09-17

Partial M3-AC03 integration; not mechanical wire policy or ordinary profile8 admission.
ZPLControlEncoder.prepareFinishingNormalization resolves against the complete exact
schema8 profile and finishing policy, then uses the same normal-control encoder as
ordinary formats. Its sealed FinishingControlNormalization retains profile, plan,
actual resolved controls and bounded bytes. No legacy profile is fabricated.
FinishingRasterPreparation stores this result before worker admission and exposes
its retained controls. Original-source/raster/profile provenance remains unchanged.

The prefix contains qualified explicit thermal, motor speed, darkness normalization,
tracking, dimensions/home and signed offsets. Schema8 reissues ^MTD/^MTT, just as
schema7 does; legacy ordinary schemas preserve previous bytes. Finishing mode
commands are intentionally not in this normal prefix: selected mode, cut/file
boundaries, copy/quantity/format wrapping and peel handling require a separate
qualified output policy. The prefix has no ^MM/^PQ/~JK/^XA/^XZ/^GF. Ordinary
resolve/control/bitmap/prepared paths remain gated for8. A prefix is not a job
payload or permission to deliver to a device.

Nearest independent constraints: retaining finishing policy cannot omit explicit
schema8 thermal normalization, and mechanical plan intent cannot be confused with
finished cut-policy wire output. Six Core and eight native focused cases passed,
covering both thermal methods, exact equality with canonical ordinary normal bytes,
all four qualified modes with forbidden finishing/format/trigger bytes, output cap,
ordinary gate and original-PDF preparation retaining exact zero/speed/home/shift bytes.
The canonical zero-darkness spelling is ~SD00; test setup corrected its ~SD0 spelling
without changing the encoder. Removing schema8 thermal normalization failed ownexit1;
source byte-restored SHA256
909cc3e14465b24aa36655ec45ba2793eac7c5df877be000a2409b9bcdce1b82.
Six restored Core and eight restored native focused cases passed ownexit0.
Finite900s full gate follows; no terminal full pass claimed.

Remaining: qualified mechanical format/cut/file framing and peel waits, accepted
queue/ticket/device identity, generic pitch qualification, adapter/lifecycle and
manual/hardware evidence. No printer/admin/GUI/merge/binary publication action.
Frozen Part B unchanged. Protocol mapping reuses R45-backed ordinary encoder facts;
no new device protocol or third-party implementation is introduced.

Full finite900s gate live session87326, log `/tmp/zpl-finishing-normalization-full.log`.
Poll this same process until its own terminal result.

## Reviewed in-progress gate

Manual source review and tracked/new-file disclosure scan passed; encoder hash
remains the recorded restored value. Same finite session87326 passed272Core
debug/release with zero failures; native/package stages remain live. No full pass
claimed. Hosted35228709904 at published3f356c5 was freshly observed in progress.

## Terminal full gate

Full finite900s session87326 completed ownexit0:89Python/272Core/323Mac debug/release,132 strict and180 ASCII oracle round trips per mode, finite benchmark/inert ABI/pipeline checks, ARM/minimum26 metadata, nested local ad-hoc signatures, unavailable Developer-ID negative and packaged-worker PBM/ZPL equality. Artifact artifacts/setup-app.I8weej. No printer accessed.
Source/disclosure review passed; restored encoder hash unchanged. No mechanical wire, installed/GUI/physical or complete-ticket qualification inferred.
