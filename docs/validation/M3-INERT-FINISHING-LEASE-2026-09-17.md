# Finite inert finishing coordinator — 2026-09-17

Partial software prerequisites for M3-AC07/08/09, not installed queue or hardware evidence.
InertFinishingDelivery connects the sealed original-source framed candidate, exact delivery
tracker and existing validated kernel lease. It has no endpoint, device handle or output
sink: bytes are discarded and status is explicitly synthetic. A supplied opaque domain is
simulator input, not accepted device admission. Production must derive ownership from the
immutable accepted ticket and verify actual correlated status and separate-file semantics.
The lease spans attempts, all byte accounting, label completion, delayed-cut readiness/
completion and peel removal including the last label. Scope exit releases on success,
unknown status, cancellation, injected failure or exception. Nonblocking contention is
explicit deviceBusy, not a false completed result. All scenario indexes, file/status fault
kinds, chunk caps and finite positive at-most60s budget are validated before acquisition.
Cancellation/deadline checks span acquisition, steps and chunks. No indefinite waiting or
watcher is introduced. Unknown synthetic status stops uncertain, never healthy/completed.
No post-attempt automatic replay is possible, including zero known accepted bytes.

Nearest independent constraint: physical ownership extends beyond conversion/file writing
through readiness and removal waits. Nine existing actual original-PDF native focused cases
passed ownexit0. The all-mode case attempts competing kernel leases at every send attempt,
byte-discard event and status wait, validates all wait indexes, unknown status at each wait,
zero-byte attempt fault, pre-attempt failure, cancellation during status, explicit busy,
invalid scenario kinds/indexes/limits and exception cleanup. Competitors succeed after
completion and failure. This is same-process descriptor contention plus existing kernel
lease implementation, not evidence of two installed virtual queues.
Releasing before status waits failed45assertions ownexit1; byte-restored nine native cases
passed ownexit0. Restored coordinator SHA256
514595cbf639463e271946189fb5acba8fbb157592ef8f7f72e7698ec4d218ba.
Logs /tmp/zpl-inert-finishing-lease-focused.log, /tmp/zpl-inert-finishing-lease-fault.log,
/tmp/zpl-inert-finishing-lease-restored.log. Full finite900s session42783 completed FULL_GATE_EXIT 0:89 Python/272 Core/324 native
debug/release tests;132 strict/180 ASCII oracle round trips per mode; finite benchmark,
inert ABI/pipeline, ARM/minimum26 metadata, nested local ad-hoc signatures, unavailable
Developer-ID negative and packaged-worker PBM/ZPL equality passed. Artifact
artifacts/setup-app.DeJXCW; log /tmp/zpl-inert-finishing-lease-full.log. No printer accessed.
Source hash remains unchanged.
Manual source review passed: bounded monotonic byte accounting, validated fault indexes,
cooperative deadline/cancellation and scope-exit lease cleanup. Tracked/new disclosure
scan found zero markers. Exact hosted coverage of this new slice remains pending.

Remaining: accepted schema8 ticket/queue/device and qualified pitch admission, durable
framed artifacts and restart recovery, actual bounded file/status adapter, scheduler retry
policy, installed ownership through child/backend lifetime and matching-accessory physical
qualification. Ordinary schema8 remains gated. Frozen B unchanged. No administrator,
printer commands/labels, merge or binary publication action.
