# Exact JSON codec integrated software baseline

Evaluated clean unpublished source: `b12e65a63cfebcce3625a249975e837dbb0bc2ee`.
Command: bash scripts/ci-swift.sh, finite 900-second caffeinate/process-group wrapper. Terminal FULL_BASELINE_EXIT 0; source checkout remained clean before/after execution. Environment: ARM64 macOS 27.0 build 26A428, Apple Swift 6.4; minimum deployment metadata remains 26.0.

Passed 106 Python tests, 299 Core tests and 364 native tests in debug and release. Do not sum build configurations as distinct cases. Native debug took 93.188 seconds; release 81.413 seconds. Each accelerator configuration passed 132 strict bitmap round-trips, 180 independent compression round-trips, 12 finite CLI cases, 15 CUPS ABI cases, 14 filter ABI cases and one inert discard pipeline. Repository/fixture integrity checks passed.

Command-line products and nested app/worker local ad-hoc signatures passed, with ARM/minimum-26 metadata. Explicit unavailable Developer-ID mode failed closed rather than falling back. Packaged worker synthetic PBM/ZPL matched the release worker exactly. Local artifact: artifacts/setup-app.ANaCKz/Label Printer Driver Setup.app. It is not uploaded or substituted for the frozen manual candidate.

This baseline includes the documented-width and endpoint-byte fixes, status function-setting grammar, late TCP callbacks, typed extraction-region budget, and shared exact JSON identity codec integration. It supersedes preceding full software baselines for this evaluated source. The independent graphics oracle is preserved; no acceptance criterion was lowered.

No printer I/O, queue installation, administrator action, manual GUI, hosted CI, merge or release occurred. Actual scheduler option/default propagation, downstream ownership and uncertain retry behavior, ad-hoc installer/helper admission, retail-host upgrade/uninstall and manual GUI/accessibility remain unobserved gates. Physical GC420d USB identity/delivery/unplug recovery, loaded sensing/current settings and output quality are unqualified. Broader accessory finishing evidence is separate from the tear-off/no-cutter baseline. Frozen M1 Part B candidate remains unchanged.

The exact integer investigation's enumerated manual codec sites are repaired and covered by focused regressions; this full run validates their integration with existing software paths. It does not assert every possible JSON/Codable consumer or complete per-ID acceptance. Source-bound ledger records from preceding source checkpoints remain historical and were not automatically promoted.
