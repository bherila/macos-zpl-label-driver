# M3-AC03 control coverage automated assessment

A-level pass at clean evaluated implementation `2d59771aba5cd27d950fde395bbccaef24fc5d9a` (local unpublished source). This evaluates the protocol mapping criterion, including both ordinary and separately qualified finishing routes. It does not establish installed or physical behavior.

| Required family | Mapping, bounds and inspected oracle |
|---|---|
| Speed | Baseline ^PRp accepts print 2/3/4 ips. Standalone ^PRp,s,b accepts independently qualified integer 2..12 parameters; the ordinary encoder and finishing normalization still restrict print to 2/3/4. Feed/backfeed require a complete qualified tuple. MotorSpeedIntegrationTests assert exact independent precedence, immutable snapshots and legacy omitted-parameter caveats; ZPLControlEncoderTests reject caller declarations that widen baseline print speeds. |
| Darkness | ^MD0 neutralizes the relative adjustment before absolute ~SD00..30. DarknessIntegrationTests enumerate every admitted integer and enforce output budgets, qualification, legacy rejection and immutable prepared snapshots. |
| Thermal | Explicit ^MTD/^MTT is qualified separately from tracking/geometry. Schema 7 ordinary and schema 8 finishing normalization require an explicit method; lower schemas do not manufacture a normalization claim. ThermalControlIntegrationTests assert both methods before original graphics alongside darkness, geometry and offsets, and reject incompatible higher-priority selection. |
| Tracking | ^MNY gap; ^MNN then ^LL for continuous length 1..32000 intersecting an explicit model/memory maximum; ^MNM,offset for black mark with -75..283 conservative offset intersecting a qualified range. Missing sensing stays unknown. Geometry/offset tests reject incompatible tracking/length and missing mark offset. Standalone tests reject conflicting tracking, duplicates and missing support. |
| Dimensions | ^PW 2..32000 is an implementation subset, further intersected with label/head/model bounds; an overbroad declared maximum rejects rather than permitting printer clamping. ^LHx,y admits 0..32000 per axis with paired explicit origin. GeometryControlIntegrationTests check independent precedence, missing bounds, clipping at both prepared/direct entry points and mixed-job substitutions. Source raster placement and physical output stock remain separate. |
| Offsets | ^LS -9999..9999 and ^LT -120..120; top and mark ranges require independent qualified bounds. OffsetControlIntegrationTests check signed clipping, canonical fields, mode/length interactions and substitution. Shift/home/top/mark are separate controls. |
| Finishing | Offline qualification maps tear/cut/peel/rewind to ^MMT/^MMC/^MMP/^MMR; ^MMC grants no cut scheduling authority. Native qualified framing maps delayed cut to ^MMD and separate ~JK files after readiness, followed by completion waits; peel uses separately qualified ^MMP,N or ^MMP and label-taken waits. The native exact-byte oracle checks all four modes, batch cuts at output 3/6/7, one ^PQ1 per already-expanded label, complete ordered framing, artifact integrity and context/limit failures. Hardware/accessory observations in these fixtures are synthetic, not actual unit evidence. |

ZPLControlProtocolCoverageTests enumerate all twelve ZPLDocumentedControl.Kind cases and all four finishing families, checking cited templates, implemented ranges, model limits, interactions and conservative reference restrictions. The table is inspection metadata, not a source of support or runtime bytes. Persistence marked modelSpecificOrUnverified remains unverified; omission/leaveUnchanged does not establish physical state isolation. GC420d remains model-documented 8 dots/mm, print 2/3/4 ips and tear-off with cutter absent, peel disabled and actual sensing/settings unknown.

Provenance: [R45 and R46](../REFERENCES.md#r45--public-zebra-ordinary-control-command-tables) record public Zebra command semantics; R46 covers continuous tracking and length. The original reference rows retain their cited model sources. No proprietary driver or bundled manual was inspected, and no new external-source fetch is claimed here.

Validation at this checkpoint: full LabelCore debug and release each executed 301 XCTest tests, zero failures, own exit 0. The subsequent Swift Testing footer reports zero tests because these suites use XCTest; it is not the XCTest count. Corrected ProfileBoundFinishingJobPlanTests debug and release each executed nine tests, zero failures, own exit 0. The first FinishingFramedOutputTests filter matched no tests and is explicitly NOT VALIDATION; it was replaced with the actual suite, without altering product code or tests. Logs are local /tmp/zpl-control-assessment-core-{debug,release}.log and /tmp/zpl-control-assessment-native-{debug,release}.log.

This assessment advances only M3-AC03 at level A. Previous full native/accelerator baselines remain historical; no new full-native or hosted-CI result is claimed. GUI/accessibility, installed scheduler/admin, USB, physical finishing/state isolation, retail minimum-runtime qualification and release evidence remain open. The frozen Part B candidate is unchanged. No printer I/O, privilege, merge or publication occurred.
