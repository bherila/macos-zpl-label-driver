# Isolated current-source software baseline

Evaluated local, unpublished source: `b3e90d22135673c7a0b6f053eeb9b823ebf3c1fa`. Working tree was clean before
and after execution. Automated local Mac27 ARM run; process wrapper enforced
900 seconds and kept the machine awake with caffeinate. Command:
`bash scripts/ci-swift.sh`. Terminal result: exit0.

This checkpoint includes saved-preview content disclosure, chooser-failure
selection revocation and bounded benchmark command/group cleanup. Dedicated
fault regressions are recorded in their slice reports; no oracle was changed.

- Python:105 tests passed.
- LabelCore:281 tests passed in debug and release.
- LabelMac:360 tests passed in debug (82.220s) and release (69.144s).
- Per-mode independent checks:132 strict and180 ASCII compression round-trips;
  12 finite encoding CLI cases;15 CUPS ABI cases;14 filter ABI cases;
  one filter-to-discard pipeline case.
- Local ad-hoc nested signatures, ARM/minimum26 metadata, explicit unavailable
  Developer-ID rejection, and packaged-worker synthetic PBM/ZPL equality passed.
- Final app artifact: `artifacts/setup-app.fAUnee/Label Printer Driver Setup.app`
  in the isolated checkout. Artifact is local and unpublished.

No printer was accessed. No queue/default change, privilege operation, manual
GUI check, installer execution, quarantine approval or physical label occurred.
Runtime27 does not establish runtime26 compatibility. Full automated success
is not production installation, scheduler, USB or physical acceptance.

Frozen M1 Part B remains pending. Its candidate and consent budget are unchanged.
The setup app's native dialogs, keyboard and VoiceOver remain NOT RUN. The
qualified USB/backend lifetime, account-free native installer, update/rollback,
uninstall, installed application printing and physical quality/fault gates remain
open. Earlier exact-source acceptance records were not refreshed by this gate;
per-ID semantic evidence review remains necessary.
